digests/
ledger.db*
*.ndjson
node_modules/
.superpowers/
# Orchestration scratch: a dispatch projection is owner-written and read-only,
# and a done marker names a candidate SHA. Neither is repository history.
.dispatch/
.done/
