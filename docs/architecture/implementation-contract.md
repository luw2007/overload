# Implementation contract — approved human decision control

Approved design: docs/architecture/human-decision-design.md (within this repository), including §16 R2/R3 overrides. User authorizes all M0–M4 development, not production activation/deploy/push. Integration workspace: a scratch worktree (outside the repo root) dedicated to this wave. All workers share this isolated workspace with DISJOINT ownership; no commits, no checkout, no formatter/lint/build/tests during wave. Owner runs acceptance at end. Add meaningful regression tests only for uncertain observable boundaries. No fake behavior, stub, TODO or silent fallback. Read existing conventions, use available LSP references before changing exported symbols.

## Ownership
Core owns new src/control/{types,store,outbox,projection}.ts and corresponding tests/schema; src/ingest/{ingest,reducer,schema.sql}.ts (actual schema filename schema.sql), src/shared/types.ts additions only for event kind, src/cli/audit.ts and audit tests (M4 metrics). Source of truth API below. Do not edit web, orchestrator, extension, decision-bot, shared/queries, CLI overload.
Execution owns src/orchestrator/* (all), integrates core API and contracts, includes M0a/M0b and M1 supersession/M3 budget enforcement. No core edits.
Authorization owns src/decision-bot/* and src/extension/overload.ts plus extension tests. Implements human_only consume revalidation, receipt effect observations, scoped policy candidates and safe promotion, contracts integration. No core/server edits.
Surface owns src/web/*, src/shared/queries.ts and tests, src/notify/* and tests, src/cli/overload.ts and tests. Implements complete M1–M4 user workflow/CLI and interface migrations. No core implementation edits. Owner owns integration fixes and docs/scripts only until all writers finish.

## Shared APIs (must implement/consume exactly; coordinate via wailmer orchestration before necessary amendments)
All core exports from src/control/store.ts, types from types.ts. import type {Database} from bun:sqlite. Existing mailbox path is control DB; core initializer ensureControlSchema(db:Database):void must work alongside openMailbox and without orchestrator tables. openControl(path?:string):Database default OVERLOAD_ANSWERS_PATH or ~/.overload/orchestrator-answers.db. No circular decision-bot imports; plain SQLite and shared utilities.
Types:
Contract={objective:string,beneficiary?:string,acceptance:Array<{id:string,kind:'check'|'artifact'|'human',description:string,evidence?:string}>,non_goals:string[],scope:{repo?:string,cwd?:string,allowed_effects?:string[],human_only_effects?:string[]},budget:{retry_limit?:number,deadline_at?:number,cost_limit?:number,cost_mode?:'hard'|'soft'|'unknown'},stop_conditions:Array<{id:string,kind:'hard'|'judgment',description:string}>,decision_owner:string}.
Work={work_id:string,title:string,source:string,source_id:string|null,state:'candidate'|'active'|'stopped'|'completed',revision:number,contract:Contract|null,created_at:number,updated_at:number}.
AttentionItem={item_id:string,work_id:string,revision:number,state:'open'|'applying'|'resolved'|'superseded',effect_state:'not_started'|'applying'|'succeeded'|'failed'|'unknown',urgency:'now'|'inbox',conclusion:string,trigger:string,impact:string,recommendation:string|null,options:string[],owner:string,expires_at:number|null,defer_until:number|null,acknowledged_at:number|null,source_link:string|null,approval_id:string|null,consumer_owner:'extension'|'orchestrator'|null,contract_revision:number,decision_mode:'human_only'|'scoped_auto',evidence:Record<string,unknown>,created_at:number,updated_at:number}.
Functions: createWork(db,input:{title:string,source:string,source_id?:string,contract?:Contract,candidate?:boolean},now?:number):Work; getWork(db,workId):Work|null; listWorks(db):Work[]; reviseContract(db,workId,expectedRevision:number,contract:Contract,reason:string,now?:number):Work; redirectWork(db,workId,expectedRevision:number,input:{reason:string,affected_work_ids:string[],action:'activate'|'pause'|'stop',evidence?:Record<string,unknown>},now?:number):Work; recordStopCondition(db,workId,conditionId:string,evidence:Record<string,unknown>,now?:number):AttentionItem.
upsertAttention(db,input: Omit<AttentionItem,'revision'|'created_at'|'updated_at'|'defer_until'|'acknowledged_at'> & {expected_revision?:number},now?:number):AttentionItem; getAttention(db,itemId):AttentionItem|null; listAttention(db,zone?:'now'|'inbox'|'done',now?:number):AttentionItem[]; actOnAttention(db,itemId,expectedRevision:number,action:'ack'|'defer'|'resolve',input?:{defer_until?:number,reason?:string},now?:number):AttentionItem. resolve must reject open external-effect approvals/unknown effects, ack never answer. recordAttentionFeedback(db,itemId,expectedRevision:number,useful:boolean,reason?:string,now?:number):void.
Failures throw ControlError with code ('not_found','conflict','invalid','blocked') and message; UI maps status 404/409/400/409.
All mutations version CAS, event emission in transaction, monotonic revisions, no stale approval. Store marks contract-invalidated attention superseded but must NOT fake consumed receipts; consumers revalidate current authoritative contract revision. Completion cannot occur by ack.

## Event transport
src/control/outbox.ts exports ensureOutbox(db):void; enqueueControlEvent(db,input:{entity_id:string,entity_version:number,kind:string,work_id?:string,item_id?:string,payload:Record<string,unknown>},now?:number):string (persistent producer identity, stable event_id); publishControlEvents(db,ledgerPath:string,emit:(detail:Record<string,unknown>)=>unknown,now?:number):{published:number,delivered:number}; caller emit writes existing envelope kind 'control_event'. Must retain until ledger applied_control_events confirms; retry durable, no duplicate projection, publisher lease, bounded retained-byte failure explicit.
src/control/projection.ts exports applyControlEvent(db,detail:Record<string,unknown>,at:number):void; called inside ingest reducer same transaction; ensure schema ledger applied_control_events event_id/payload_hash and control_attention projection. Payload identity mismatch fails safe. Projection materializes attention snapshots for ledger-only audit and notification; contracts source state retained control DB. Add event kind control_event to shared/types and validate ingest (current envelope validation path).
Store mutations enqueue events; publishers must be actually driven by Surface server poll/CLI and Execution tick. Surface queries core authoritative items for UI, notify consumes ledger projection, metrics audit only ledger events. Legacy non-control events continue supported, no duplicate cards for linked legacy approvals. Source lifecycle producer feeds same work/item IDs.

## Receipt/effect cross-task contract
Authorization adds decision_mode:'human_only'|'scoped_auto' optional default human_only to ApprovalTarget registration; default existing policy-target registration must preserve explicit scoped policy behavior only when caller supplies scoped_auto. Add optional work_id/contract_revision via scope, consume checks authoritative Work revision when bound. Human answer unaffected bot-disabled. Surface passes authority fields only from trusted server/core policy, never downgrades based on browser input.
Extension emits control_event effect_observed with receipt_id,toolCallId,attempt identity and actual tool_result evidence, via persistent outbox if available on host; no new direct arbitrary success API. Authorization exposes reconcileEffectEvents(mailbox:Database,ledgerPath:string,now?:number):void to read ledger events and update validated matching outstanding receipts, including unknown on expired missing outcome; Surface server and Execution tick call it. They call only after function exists; no temporary no-op. Tool failure != no side effect, approval != execution success. Untrusted telemetry remains same-UID workflow boundary not security sandbox.
Surface displays effect facts from AttentionItem.evidence confirmed_effects/pending_or_unknown_effects. Execution binds work_id/task contract_revision, emits terminal/blocked states through core event path. No hidden successor task; CI actions recheck/manual-followup/abandon with exact effects; invalidate old targets rather than aliasing old answers.

## Delivery
Each worker implements whole owned behavior with tests but skips running validation. Report files changed, exported API, known missing cross-task integration and exact smoke steps, not claims of full completion based on unit code. Use Wailmer lifecycle worker_done. Ask owner for contract changes; never invent alternate API. Source review no tests until all mutations settle. No production config mutations. Any worker limitation must be explicit and owner resolves before final delivery.
