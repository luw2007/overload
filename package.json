{
  "name": "overload-n3-tests",
  "version": "0.0.0",
  "private": true,
  "description": "N3 P1 acceptance layer: V-2' behavior matrix + injection tests (test-only).",
  "scripts": {
    "test": "bun test test/"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
