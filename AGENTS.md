# Overload 产品与工程指导原则

## 产品核心

Overload 的核心是解决人类注意力分配问题。Agent 生命周期采集、分类、提醒、跳转和归档都是实现这一目标的手段，不是产品目标本身。

Overload 必须把大量 Agent 运行噪声压缩为少量、适时、可执行、可续跑的人类决策。

## 理想闭环

```text
Agent 执行
→ 自动消化常态
→ 识别需要人介入的风险、歧义、异常或最终验收
→ 压缩为可决策工作项
→ 路由给唯一责任人
→ 人类决策
→ 从原现场续跑
→ 结果主动回流并归档
```

## 注意力调度原则

1. **仅在必要时打断。** 只有风险、歧义、不可恢复失败和最终验收应主动占用人类注意力；普通进度不得打断。
2. **只找正确的人。** 优先路由给指派人、评论作者、资源 Owner 或当班角色；不得用群体广播替代责任归属。
3. **区分现在与稍后。** 紧急事项立即呈现；非紧急事项进入可批量处理的 Inbox；有时效的决策必须显示有效期。
4. **压缩而非转发噪声。** 同一任务的连续事件必须聚合、去重并原地更新。呈现结论、证据、影响面、建议动作和 deep link，隐藏过程噪声。
5. **让决策可直接执行。** 低风险事项自动处理；高风险事项使用结构化确认；需要人处理的工作项必须明确说明“要决定什么”。
6. **保留现场连续性。** Session、thread、checkpoint、运行历史和绑定关系必须足以让用户一步回到原现场，并让 Agent 在决策后从原 checkpoint 续跑。续跑仅保证于进程已终止的场景；仍在阻塞等待（如 blocked-on-ask）的活会话应跳转至其现场，而非静默重启。
7. **异常必须有预算。** 只对已识别的瞬时故障做有限自动恢复；预算耗尽后保留现场、明确失败分类，并给出恢复动作。不得无限重试、静默失败或要求用户从头重建上下文。
8. **完成必须主动回流。** 完成、失败和决策后的恢复结果必须回到原工作项，并在不再需要注意力时自动归档。

## 产品界面原则

面向用户的首要信息架构应表达注意力状态，而非暴露内部分类：

- **Now**：必须立即处理。
- **Inbox**：可以稍后批量处理。
- **Done**：已经决策、恢复或归档。

Q1–Q5 等队列是内部分类和实现机制。新增界面不得默认要求用户理解内部队列，除非该分类本身是完成决策所必需的证据。

## 决策卡最小载荷

每个需要人处理的工作项必须尽量在一个视图内提供：

- 一句话结论；
- 触发原因及关键证据；
- 不处理或不同选择的影响；
- 明确的建议动作或结构化选项；
- 唯一责任人与时效；
- 返回原任务现场的入口；
- 决策后的续跑状态。

## 优先级判断

评估需求或实现方案时，按以下顺序判断：

1. 它是否减少无效打断？
2. 它是否减少用户理解和重建现场的成本？
3. 它是否让正确的人更快完成正确决策？
4. 它是否保证决策后能可靠续跑并回流结果？
5. 若以上皆否，它是否只是增加了监控信息或状态展示？若是，默认不做。

## 产品指标

优先衡量：

- 无效打断数；
- 待决策停留时间；
- 完成一次决策所需的阅读量和操作数；
- 从提醒回到原现场的耗时；
- 决策后成功续跑率；
- 同任务通知压缩率；
- 已结束事项自动清退率。

采集事件数、支持的 Agent 数和展示的状态数只能作为能力或运行指标，不得替代注意力效果指标。
